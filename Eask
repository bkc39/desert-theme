;; -*- mode: eask; lexical-binding: t -*-

(package "desert-theme"
         "0.1.0"
         "A warm earthy port of Vim's desert theme")

(website-url "https://github.com/bkc39/desert-theme")
(keywords "faces" "theme")

(package-file "desert-theme.el")

(source "gnu")
(source "melpa")

(depends-on "emacs" "27.1")

;; Treat byte-compile warnings as failures (MELPA expects a clean compile).
(setq byte-compile-error-on-warn t)
